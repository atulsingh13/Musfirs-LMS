import { CrmDashboard } from "@/components/dashboard/crm-dashboard";

/** Manager dashboard — same CRM KPI grid as Admin (API-scoped). */
export function ManagerDashboard() {
  return <CrmDashboard />;
}
