/**
 * Convenience re-export for user profile / permissions mock data.
 * Canonical source: `@/lib/static-data/user-permissions`
 */
export {
  ATUL_SINGH_PROFILE,
  MODULE_PERMISSIONS,
  PERMISSION_SOURCES,
  PERMISSION_SUMMARY,
  RECENT_PERMISSION_CHANGES,
  RESTRICTED_PERMISSIONS,
} from "@/lib/static-data/user-permissions";
